# Core dependencies
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# PFNs is installed separately via setup.sh:
#   git clone https://github.com/automl/PFNs.git
#   cd PFNs && pip install -e .

# Varianta A (segmentace, experiments/Experiment_A_UniverSeg_segmentation.ipynb) — instaluj zvlášť:
#   pip install git+https://github.com/JJGO/UniverSeg.git   # zmražený in-context segmenter
#   pip install scipy nibabel                                # boundary band + OASIS (nifti)
# Data se stahují automaticky do /tmp (WBC z github, OASIS z surfer.nmr.mgh.harvard.edu).

# Optional: for examples only
# tabpfn>=0.1.9
# pymc>=5.0.0
# pytensor>=2.18.0
# huggingface_hub>=0.19.0
